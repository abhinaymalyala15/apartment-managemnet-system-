/** localStorage keys — replace with httpOnly cookies when backend is connected */
export const RESIDENT_SESSION_STORAGE_KEY = "apartment_erp_resident_session";
export const RESIDENT_ACCOUNTS_STORAGE_KEY = "apartment_erp_resident_accounts";

/** Cookie name reserved for middleware session checks (Phase 2) */
export const RESIDENT_SESSION_COOKIE = "resident_session";

export const RESIDENT_REGISTER_SUCCESS_REDIRECT_MS = 4000;
