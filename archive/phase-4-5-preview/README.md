# Phase 4–5 Preview Components

These components were built during the initial MVP prototype. They are **not active** in Phase 1.

Reuse when implementing:
- **Phase 4:** `resident/resident-dashboard.tsx`
- **Phase 5:** `inspector/flats-table.tsx`

Update imports to use `DashboardLayout` and current component paths.
